/**
 * @swagger
 * components:
 *   schemas:
 *     Login:
 *       type: object
 *       properties:
 *         email:
 *           type: string
 *           description: user login
 *         password:
 *           type: string
 *           description: user password
 *       required:
 *         - username
 *         - password
 */
